import { Renderer2, AfterViewChecked, OnDestroy, AfterViewInit } from '@angular/core';
import { FieldType } from '@ngx-formly/material/form-field';
import { MatCheckbox } from '@angular/material/checkbox';
import { FocusMonitor } from '@angular/cdk/a11y';
export declare class FormlyFieldCheckbox extends FieldType implements AfterViewInit, AfterViewChecked, OnDestroy {
    private renderer;
    private focusMonitor;
    checkbox: MatCheckbox;
    defaultOptions: {
        templateOptions: {
            hideFieldUnderline: boolean;
            indeterminate: boolean;
            floatLabel: string;
            hideLabel: boolean;
            align: string;
            color: string;
        };
    };
    private _required;
    constructor(renderer: Renderer2, focusMonitor: FocusMonitor);
    onContainerClick(event: MouseEvent): void;
    ngAfterViewInit(): void;
    ngAfterViewChecked(): void;
    ngOnDestroy(): void;
}
